	float horizontal = Input.GetAxis("Horizontal");
	float vertical = Input.GetAxis("Vertical");

        player.AddForce(Vector3.forward * speed * vertical);
        player.AddForce(Vector3.right * speed * horizontal);


        player.AddForce(Vector3.forward * speed * vertical, ForceMode.Impulse);
        player.velocity = Vector3.right * 50 * horizontal;

	transform.Translate(Vector3.forward * vertical * speed * Time.deltaTime);
        transform.Translate(Vector3.right * horizontal * speed * Time.deltaTime);
       transform.position = transform.position +  Vector3.right * 12 * Time.deltaTime;
	transform.localscale = new vector2 (1,1);
	transform.rotation = Quaternion.LookRotation(Vector3.right);


        if (Input.GetKey(KeyCode.W))
        {
            player.velocity = Vector3.forward * speed;
	transform.Translate(Vector3.forward * speed * Time.deltaTime);
        }

        if (Input.GetKey(KeyCode.S))
        {
            player.velocity = Vector3.back * speed;
	transform.Translate(Vector3.back * speed * Time.deltaTime);
        }

        if (Input.GetKey(KeyCode.D))
        {
            player.velocity = Vector3.right * speed;
	transform.Translate(Vector3.right * speed * Time.deltaTime);
        }
        if (Input.GetKey(KeyCode.A))
        {
            player.velocity = Vector3.left * speed;
	transform.Translate(Vector3.left * speed * Time.deltaTime);
        }


 	Vector3 move = transform.right * horizontal + transform.forward * vertical;
        controller.Move(move * speed * Time.deltaTime);


			first person mouse
	Cursor.lockState = CursorLockMode.Locked;

	Vector3 x = target.position + offset;
        Vector3 y = Vector3.Lerp(transform.position, x, speed);
        transform.position = y;

        float mouseX = Input.GetAxis("Mouse X") * mouseSensitivity * Time.deltaTime;
        float mouseY = Input.GetAxis("Mouse Y") * mouseSensitivity * Time.deltaTime;

        xRotation -= mouseY;
        xRotation = Mathf.Clamp(xRotation, -90f, 90f);

        transform.localRotation = Quaternion.Euler(xRotation, 0f, 0f);
        target.Rotate(Vector3.up * mouseX);

			Camera moves Player.

	[SerializeField] Transform target;
    	[SerializeField] Vector3 offset;
    	private readonly float speed = 0.125f;
    	[SerializeField] Transform background;

	Vector3 x = target.position + offset;
        Vector3 y = Vector3.Lerp(transform.position, x, speed);
        transform.position = y;


        // lock Y position
        transform.position = new Vector3(background.position.x, 0, 		          
	transform.position.z);
        //transform.position = background.position;

	IEnumerator Random()
        {
            yield return new WaitForSeconds(2);
            StartCoroutine(Random);

        }
    	Instantiate();
    	InvokeRepeating();